<%= form_with(model: cake_blog, local: true) do |form| %>
  <% if cake_blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cake_blog.errors.count, "error") %> prohibited this cake_blog from being saved:</h2>

      <ul>
        <% cake_blog.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
